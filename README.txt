To install all the packages, look in 'requirements.txt'
(It was not preinstalled as the file would be too large to submit on EBART)

to run the web application, type in the terminal:
"streamlit run main.py"